<%- include("header") -%>

  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>


  
  <div class="box">

    <form class="item" action="/" method="post">
      <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
      <button type="submit" name="list">+</button>
    </form>

    <% newListItems.forEach(function (eachIteam) { %>
      <form action="/delete" method="post">
          <div class="item">
            <input type="checkbox">
            <p><%=  eachIteam.name  %></p>
            <button class="delete" type="submit" name="removen" value="<%= eachIteam._id %>">❌</button>
          </div>
        </form>
        
        <% }) %>

      
      

      
  </div>

<%- include("footer") -%>
